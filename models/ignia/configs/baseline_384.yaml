run_name: "baseline-deeplabv3plus-384"
data_root: "../../data/cityscapes"
use_modified: false

# training — matched to modified model config
epochs: 20
batch_size: 4
accumulation_steps: 2
crop_size: 384
lr: 0.0001
weight_decay: 0.01
num_workers: 4